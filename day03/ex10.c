#include <stdio.h>

int main() {

    /*

    조건문

    // 조건이 한개일 경우
    if(조건식/변수) {
        조건식이 참일 때만 실행되는 code
    }

    // 조건이 두개일 경우
    if(조건식/변수) {
        조건식이 참일 때만 실행되는 code
    } else {
        조건식이 거짓일 때만 실행되는 code
    }
    */

    int number1 = 10;
    if(number1 == 11)

    printf("number1은 %d입니다\n", number1);
    printf("number1은 홀수입니다\n");
    printf("프로그램 종료");

    printf("프로그램 종료");
    return 0;

}
